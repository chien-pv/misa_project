<section class="content">
  <div class="wrapper">
    <div class="page-header">
      <h3 align="center">DANH SÁCH ĐƠN HÀNG</h3>
    </div>
    <div class="">
      <h4>Chọn đơn hàng cần dùng</h4>
      <%= form_tag(map_order_index_path) do %>

        <%= select_tag 'list_orders', options_for_select(@purchase_order.collect{ |u| [u.RefNo, u.RefID] }), multiple: true , id: :purchase_orders%>

         <center style="margin-top: 10px"><%= submit_tag( "Gom Container" , class: "btn btn-primary" ) %></center>
      <% end %>
    </div>
    <p id="demo"></p>
  </div>
</section>
<script src="asset/header.js"></script>